inter_gols = gremio_gols = 0

print(inter_gols, gremio_gols)

inter_gols, gremio_gols = input('Gols: ').split()
inter_gols = int(inter_gols)
gremio_gols = int(gremio_gols)

print('Novo grenal (1-sim 2-nao)')
resp = input()
